# Assets del hero principal

- Fondo: agrega la fotografía final como `villas-hero.jpg` en esta misma carpeta.
- Logo: reemplaza `villas-logo.svg` por el archivo final conservando el mismo nombre, o actualiza el `src` en `app/(site)/page.tsx`.
- Palmera de la introducciÃ³n: reemplaza `intro-palm-logo.svg` por tu logo final conservando exactamente ese nombre.

La composición ya usa `/images/home/villas-hero.jpg` y `/images/home/villas-logo.svg`. Para el fondo se recomienda una imagen horizontal de al menos 1920 × 1080 px, optimizada para web.
